# Notes about running scenarios on BlueSky

When running scenarios with CROFF ensure that the SPEEDBASEDM2.py code gets modified by commenting out the line that adjusts the speeds
in the "resolve()" function
